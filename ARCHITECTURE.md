# 🏗️ Architecture Diagram

## Dynamic Configuration System

```
┌─────────────────────────────────────────────────────────────────┐
│                        YOUR REACT NATIVE APP                     │
│                     (Now 100% Customizable!)                     │
└─────────────────────────────────────────────────────────────────┘
                                 │
                    ┌────────────▼─────────────┐
                    │  src/config/appConfig.js │
                    │  ═══════════════════════ │
                    │  ⭐ SINGLE SOURCE OF     │
                    │     TRUTH FOR ALL        │
                    │     CUSTOMIZATION        │
                    └─────────────┬────────────┘
                                  │
        ┌─────────────────────────┼──────────────────────────┐
        │                         │                          │
        ▼                         ▼                          ▼
┌───────────────┐        ┌────────────────┐        ┌───────────────┐
│   App.js      │        │ CounterScreen  │        │ Other Screens │
│ ───────────── │        │ ────────────── │        │ ───────────── │
│ • Navigation  │        │ • Display      │        │ • Stats       │
│ • Header      │        │ • Input        │        │ • Profile     │
│ • Tabs        │        │ • Quotes       │        │ • Auth        │
│ • Icons       │        │ • Colors       │        │               │
└───────────────┘        └────────────────┘        └───────────────┘
```

---

## Configuration Structure

```
appConfig.js
│
├── App Identity
│   ├── appName: "राम Bank"
│   ├── mantraWord: "राम"
│   └── mantraWordEnglish: "Ram"
│
├── Colors (Theme System)
│   ├── primary: "#FF9933"
│   ├── secondary: "#138808"
│   ├── accent: "#D4A373"
│   ├── background: "#FFFFFF"
│   ├── surface: "#F5F5F5"
│   ├── text: "#333333"
│   └── ... more colors
│
├── Navigation
│   ├── counter
│   │   ├── icon: "checkmark-circle"
│   │   ├── iconOutline: "checkmark-circle-outline"
│   │   └── label: "Count"
│   ├── stats
│   └── profile
│
├── Quotes Array
│   ├── [0] { text, translation }
│   ├── [1] { text, translation }
│   └── ... more quotes
│
├── Counter Settings
│   ├── milestoneInterval: 10
│   ├── milestoneEmoji: "🎉"
│   ├── spiritualIcon: "🕉️"
│   ├── autoIncrementEnabled: true
│   └── showAnimation: true
│
├── Text Content
│   ├── counterScreen
│   │   ├── todayLabel
│   │   ├── todaySubtext (with {mantra} template)
│   │   ├── inputLabel
│   │   ├── milestoneMessage (with {count} {mantra})
│   │   └── ... more text
│   ├── statsScreen
│   └── profileScreen
│
└── Features (Toggle on/off)
    ├── showQuotes: true
    ├── showMotivation: true
    ├── showMilestones: true
    ├── showStats: true
    └── enableAnimations: true
```

---

## Data Flow

```
┌──────────────────┐
│  User Edits      │
│  appConfig.js    │
└────────┬─────────┘
         │
         │ 1. Import config
         ▼
┌──────────────────┐
│  Component       │
│  (App.js, etc)   │
└────────┬─────────┘
         │
         │ 2. Use values
         ▼
┌──────────────────┐         ┌──────────────────┐
│  appConfig.      │────────▶│  Template String │
│  colors.primary  │         │  Replace {mantra}│
└────────┬─────────┘         └────────┬─────────┘
         │                            │
         │ 3. Render                  │
         ▼                            ▼
┌──────────────────┐         ┌──────────────────┐
│  UI Element      │         │  Dynamic Text    │
│  with color      │         │  "राम chants"    │
└──────────────────┘         └──────────────────┘
```

---

## Template String Pattern

```
Config:
┌────────────────────────────────────────┐
│ todaySubtext: '{mantra} chants today'  │
│ mantraWord: 'राम'                      │
└────────────────────────────────────────┘
                  │
                  │ .replace('{mantra}', appConfig.mantraWord)
                  ▼
┌────────────────────────────────────────┐
│ Result: 'राम chants today'             │
└────────────────────────────────────────┘
```

---

## Feature Flag Pattern

```
Config:
┌───────────────────────┐
│ features: {           │
│   showQuotes: true    │───┐
│ }                     │   │
└───────────────────────┘   │
                            │
Component:                  │
┌───────────────────────┐   │
│ {appConfig.features.  │◀──┘
│  showQuotes && (      │
│   <QuoteBox />        │───▶ Renders if true
│ )}                    │    Hidden if false
└───────────────────────┘
```

---

## File Structure

```
ram-counter-mobile/
│
├── src/
│   ├── config/
│   │   ├── appConfig.js          ⭐ EDIT THIS!
│   │   ├── theme.js              (spacing, shadows)
│   │   └── api.js
│   │
│   ├── screens/
│   │   ├── app/
│   │   │   ├── CounterScreen.js  (uses appConfig)
│   │   │   ├── StatsScreen.js    (uses appConfig)
│   │   │   └── ProfileScreen.js  (uses appConfig)
│   │   └── auth/
│   │
│   └── components/
│
├── App.js                        (uses appConfig)
│
├── Documentation Files:
├── START_HERE.md                 👈 Read first!
├── QUICK_START.md                (5 min guide)
├── CONFIGURATION.md              (Complete reference)
├── THEME_PRESETS.js              (8 ready themes)
├── BEFORE_AFTER.md               (See improvements)
├── README_DYNAMIC.md             (Architecture)
└── IMPLEMENTATION_SUMMARY.md     (What changed)
```

---

## Theme Switching Flow

```
Step 1: Choose Theme
┌──────────────────┐
│ THEME_PRESETS.js │
│ • ramTheme       │
│ • krishnaTheme   │──┐
│ • darkTheme      │  │
│ • minimalTheme   │  │
└──────────────────┘  │
                      │
Step 2: Copy          │
                      ▼
┌──────────────────────────────┐
│ Copy theme object            │
└──────────────┬───────────────┘
               │
Step 3: Paste  │
               ▼
┌──────────────────────────────┐
│ src/config/appConfig.js      │
│ export default {             │
│   ...krishnaTheme,           │
│ }                            │
└──────────────┬───────────────┘
               │
Step 4: Reload │
               ▼
┌──────────────────────────────┐
│ 🎉 New Theme Applied!        │
└──────────────────────────────┘
```

---

## Color System

```
appConfig.colors
│
├── primary ──────┬─▶ Header Background
│                 ├─▶ Counter Circle
│                 ├─▶ Input Border
│                 └─▶ Main Buttons
│
├── secondary ────┬─▶ Motivation Border
│                 └─▶ Secondary Actions
│
├── accent ───────┬─▶ Tab Highlight
│                 └─▶ Quote Gradient
│
├── background ───┬─▶ Screen Background
│                 └─▶ Card Base
│
├── surface ──────┬─▶ Card Background
│                 └─▶ Input Fields
│
└── text ─────────┬─▶ Primary Text
                  └─▶ Labels
```

---

## Comparison: Before vs After

```
BEFORE (Hardcoded)                AFTER (Dynamic)
══════════════════                ═══════════════

Multiple Files                    Single File
┌──────────┐                     ┌──────────────┐
│ App.js   │ '#FF9933'           │ appConfig.js │
│ Counter  │ '#FF9933'     ────▶ │ primary:     │
│ Stats    │ '#FF9933'           │  '#FF9933'   │
│ Profile  │ '#FF9933'           │              │
└──────────┘                     └──────────────┘
❌ Scattered                      ✅ Centralized

Direct Values                     Templates
┌──────────────────┐             ┌──────────────────┐
│ 'राम chants'     │      ────▶  │ '{mantra} chants'│
│ 'राम Bank'       │             │ appName          │
└──────────────────┘             └──────────────────┘
❌ Hardcoded                      ✅ Dynamic

No Control                        Feature Flags
┌──────────────────┐             ┌──────────────────┐
│ <QuoteBox />     │      ────▶  │ showQuotes: true │
│ Always renders   │             │ Toggle on/off    │
└──────────────────┘             └──────────────────┘
❌ Fixed                          ✅ Flexible
```

---

## Benefits Diagram

```
                    ┌──────────────────┐
                    │  appConfig.js    │
                    └────────┬─────────┘
                             │
           ┌─────────────────┼─────────────────┐
           │                 │                 │
           ▼                 ▼                 ▼
    ┌───────────┐    ┌───────────┐    ┌───────────┐
    │  Simple   │    │  Dynamic  │    │   Safe    │
    │  No logic │    │  Instant  │    │   Hard to │
    │  required │    │  changes  │    │   break   │
    └───────────┘    └───────────┘    └───────────┘
           │                 │                 │
           └─────────────────┼─────────────────┘
                             ▼
                    ┌──────────────────┐
                    │  Happy Developer │
                    │  Happy User      │
                    └──────────────────┘
```

---

## Summary

```
┌─────────────────────────────────────────────────┐
│  ONE FILE = COMPLETE CONTROL                    │
│                                                 │
│  src/config/appConfig.js                        │
│  ↓                                              │
│  • Change anything                              │
│  • Instantly                                    │
│  • Safely                                       │
│  • Simply                                       │
│                                                 │
│  NO COMPLEX LOGIC NEEDED! 🎯                    │
└─────────────────────────────────────────────────┘
```

---

**This is the power of configuration-based architecture!** 🚀

node_modules/
.env
.env.local
.env.*.local
.expo/
.expo-shared/
dist/
build/
android/
ios/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.env.example.local
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# EAS local signing credentials (points at debug.keystore)
credentials.json

# Secrets / env (never commit)
.env.production
.env.*.production

# Local build logs and run logs
*.log

# Local JDK / large binaries (portable toolchain, not source)
jdk17/
jdk17.zip
.jdks/

# Claude Code local session/memory dir
.claude/

# Local build-output check dir
dist-check/

# Local dev tooling / downloaded Android cmdline-tools (binary, not source)
.tools/

# Google Play service-account key for eas submit — NEVER commit
play-service-account.json
*service-account*.json
